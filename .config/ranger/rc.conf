# Viewmode 
# miller: columns show multiple levels of hierarchy
# multipane: Midnight Commander style tabs
set viewmode miller

# How many colums and relative width
set column_ratios 0

set show_hidden true

set confirm_on_delete multiple

# Truncate long commit message in the status bar
set vcs_msg_length 30

set show_hidden_bookmarks true

# Which colorscheme to use? available by default
# default, jungle, snow, solarized
set colorscheme default

# Preview files on the rightmost column?
# and collapse (shrink) the last if nothing to preview
set preview_files true
set preview_directories true
set collapse_preview true

# Wrap long lins in plain txt previews
set wrap_plaintext_previews true

# Save console history on exit?
set save_console_history true

# Draw status bar on top of the browser window (default: bottom)
set status_bar_on_top false

# Draw progress bar in status bar displaying average state of all
set draw_progress_bar_in_status_bar true

# Draw borders around colums? (seperators, outlin, both or none)
set draw_borders false

# Display the directory name in tabs?
set dirname_in_tabs false

# Display file size in main column or status bar
set display_size_in_main_column true
set display_size_in_status_bar true

# Display free disk space in status bar
set display_free_space_in_status_bar true

# Display files tags in all columns or only in main column?
set display_tags_in_all_columns true

# Set a title for the window? Updates both `WM_NAME` and `WM_ICON_NAME`
set update_title false

# Set the tmux/screen window-name to "ranger"?
set update_tmux_title true

# Shorten the title if it gets long?  The number defines how many
# directories are displayed at once, 0 turns off this feature.
set shorten_title 3

# Show hostname in titlebar?
set hostname_in_titlebar false

# Abbreviate $HOME with ~ in the titlebar (first line) of ranger?
set tilde_in_titlebar true

# How many directory-changes or console-commands should be kept in history?
set max_history_size 20
set max_console_history_size 50

# Try to keep so much space between the top/bottom border when scrolling:
set scroll_offset 8

# Padding on the right when there's no preview?
# This allows you to click into the space to run the file.
set padding_right false

# One of: size, natural, basename, atime, ctime, mtime, type, random
set sort natural

# Additional sorting options
set sort_reverse false
set sort_case_insensitive true
set sort_directories_first true
set sort_unicode false

# Changes case sensitivity for the cd command tab completion
set cd_tab_case sensitive

# Use fuzzy tab completion with the "cd" command. For example,
# ":cd /u/lo/b<tab>" expands to ":cd /usr/local/bin".
set cd_tab_fuzzy false

# Avoid previewing files larger than this size, in bytes.  Use a value of 0 to
# disable this feature.
set preview_max_size 0

# The key hint lists up to this size have their sublists expanded.
# Otherwise the submaps are replaced with "...".
set hint_collapse_threshold 10

# Add the highlighted file to the path in the titlebar
set show_selection_in_titlebar true

# The delay that ranger idly waits for user input, in milliseconds, with a
# resolution of 100ms.  Lower delay reduces lag between directory updates but
# increases CPU load.
set idle_delay 2000

# Disable displaying line numbers in main column.
# Possible values: false, absolute, relative.
set line_numbers false

# When line_numbers=relative show the absolute line number in the
# current line.
set relative_current_zero false

# Start line numbers from 1 instead of 0
set one_indexed false

# Save tabs on exit
set save_tabs_on_exit false

# Enable scroll wrapping - moving down while on the last item will wrap around to
# the top and vice versa.
set wrap_scroll true

# Print file sizes in bytes instead of the default human-readable format.
set size_in_bytes false

# Custom Keybinds

map gh cd ~
#map gd cd ~/storage/shared/Documents
#map g. cd ~/storage/shared/Documents/dotfiles-termux/
#map gD cd ~/storage/shared/Downloads
map gc cd ~/.config
#map gv cd ~/storage/shared/Videos
#map gm cd ~/storage/shared/Music
#map gp cd ~/storage/shared/Pictures

